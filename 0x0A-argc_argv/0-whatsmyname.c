#include "main.h"
#include <stdio.h>

/**
 * main - function that prints name
 * @argc: first argument
 * @argv: second argument
 * Return: 0
 */
int main(int argc, char *argv[])
{
(void) argc;
printf("%s\n", *argv);
return (0);
}
