#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - function that returns length
 * @s: pointer variable
 * Return: void
 */

int _strlen_recursion(char *s)
{
int a;
a = strlen(s);
return (a);
}
