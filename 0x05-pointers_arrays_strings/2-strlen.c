#include "main.h"
#include <stdio.h>
/**
 * _strlen - is a pointer variable to a character
 * Return: Nothing
 * @s: input character parameter
 */
int _strlen(char *s)
{
int a;
a = 0;
while (s[a] != '\0')
{
a++;
}
return(0);
}
