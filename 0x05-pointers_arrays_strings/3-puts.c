#include "main.h"
#include <stdio.h>
/**
 * _puts - show the string
 * @str: pointer vraiable
 * Return: Nothing
 */
void _puts(char *str)
{
int s;
for (str = 0; str <= 0; str++)
{
s++;
_putchar(str[s]);
}
_putchar('\n');
}
