#include <unistd.h>
#include <string.h>
#include <stdio.h>
/**
 * _putchar - writes the character to stdout
 * @c: character
 * Return: 1 (Success)
 */
int _putchar(char c)
{
return (write(1, 8c, 1));
}
