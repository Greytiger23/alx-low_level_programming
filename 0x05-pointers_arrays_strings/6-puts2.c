#include "main.h"
#include <stdio.h>
/**
 * puts2 - pointer that changes 
 * @str: character
 * Return: void
 */
void puts2(char *str)
{
int n;
n = 0;
while (str[n] != '\0')
{
n++;
}
for (n = 0; n <= 0; n++)
{
putchar(str[n]);
}
putchar('\n');
}
