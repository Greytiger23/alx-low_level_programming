#include <stdio.h>

/**
 * main - Entry point
 * this program is printing a message with proper grammer
 * Return: Always 0 (Success)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
